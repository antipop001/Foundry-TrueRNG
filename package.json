{
  "name": "foundry-truerandom",
  "version": "2.5.0",
  "description": "True random number generator integration for Foundry VTT using random.org",
  "main": "dist/TrueRandom.js",
  "scripts": {
    "build": "tsc",
    "test": "node tests/truerandom.test.mjs",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
